services:
  - type: web
    name: tinylink-api
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    rootDir: server
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
    healthCheckPath: /healthz
